package br.ufes.inf.lprm.context.model;

rule PerceivedIntrinsicContextUpdate
    no-loop
    when
        context: Context()
        reading: Reading(id == context.id)
    then
        context.setValue(reading);
        retract(context);
        insert(context);
    end